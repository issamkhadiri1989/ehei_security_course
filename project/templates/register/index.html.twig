{% extends 'base.html.twig' %}
{% block body %}
    <fieldset>
        <legend>Create new account: </legend>
        {{ form_start(registration_form) }}
        {{ form_row(registration_form) }}
        <button type="submit" class="btn btn-primary p-2 mt-2">
            Register
        </button>
        {{ form_end(registration_form) }}
    </fieldset>
{% endblock %}
